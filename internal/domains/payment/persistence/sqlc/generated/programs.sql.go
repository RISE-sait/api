// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: programs.sql

package db_payment

import (
	"context"

	"github.com/google/uuid"
	"github.com/shopspring/decimal"
)

const enrollCustomerInProgramEvents = `-- name: EnrollCustomerInProgramEvents :exec
INSERT INTO events.customer_enrollment (customer_id, event_id)
SELECT $1, -- Customer ID parameter
       e.id
FROM events.events e
         LEFT JOIN public.schedules s ON e.schedule_id = s.id
WHERE s.program_id = $2
  AND e.start_at >= current_timestamp
`

type EnrollCustomerInProgramEventsParams struct {
	CustomerID uuid.UUID     `json:"customer_id"`
	ProgramID  uuid.NullUUID `json:"program_id"`
}

func (q *Queries) EnrollCustomerInProgramEvents(ctx context.Context, arg EnrollCustomerInProgramEventsParams) error {
	_, err := q.db.ExecContext(ctx, enrollCustomerInProgramEvents, arg.CustomerID, arg.ProgramID)
	return err
}

const getProgram = `-- name: GetProgram :one
SELECT id, name
FROM program.programs
WHERE id = $1
`

type GetProgramRow struct {
	ID   uuid.UUID `json:"id"`
	Name string    `json:"name"`
}

func (q *Queries) GetProgram(ctx context.Context, id uuid.UUID) (GetProgramRow, error) {
	row := q.db.QueryRowContext(ctx, getProgram, id)
	var i GetProgramRow
	err := row.Scan(&i.ID, &i.Name)
	return i, err
}

const getProgramIsFull = `-- name: GetProgramIsFull :one
SELECT COUNT(ce.id) >= p.capacity AS is_full
FROM program.programs p
         LEFT JOIN
     public.schedules s ON p.id = s.program_id
         LEFT JOIN
     events.events e ON s.id = e.schedule_id
         LEFT JOIN
     events.customer_enrollment ce ON e.id = ce.event_id
WHERE p.id = $1
group by p.capacity
`

func (q *Queries) GetProgramIsFull(ctx context.Context, id uuid.UUID) (bool, error) {
	row := q.db.QueryRowContext(ctx, getProgramIsFull, id)
	var is_full bool
	err := row.Scan(&is_full)
	return is_full, err
}

const getProgramRegistrationPricesForCustomer = `-- name: GetProgramRegistrationPricesForCustomer :one
WITH active_membership_id AS
         (SELECT mp.membership_id
          FROM public.customer_membership_plans cmp
                   LEFT JOIN membership.membership_plans mp ON mp.id = cmp.membership_plan_id
                   LEFT JOIN membership.memberships m ON m.id = mp.membership_id
          WHERE customer_id = $2
            AND status = 'active'
          ORDER BY cmp.start_date DESC
          LIMIT 1)

SELECT
    -- Member program price (if available)
    (SELECT ef.program_price
     FROM public.enrollment_fees ef
     WHERE ef.program_id = p.id
       AND ef.membership_id = (SELECT membership_id FROM active_membership_id))
           AS member_program_price,

    -- Member drop-in price (if available)
    (SELECT ef.drop_in_price
     FROM public.enrollment_fees ef
     WHERE ef.program_id = p.id
       AND ef.membership_id = (SELECT membership_id FROM active_membership_id))
           AS member_drop_in_price,

    -- Non-member program price
    (SELECT ef.program_price
     FROM public.enrollment_fees ef
     WHERE ef.program_id = p.id
       AND ef.membership_id IS NULL)
           AS non_member_program_price,

    -- Non-member drop-in price
    (SELECT ef.drop_in_price
     FROM public.enrollment_fees ef
     WHERE ef.program_id = p.id
       AND ef.membership_id IS NULL)
           AS non_member_drop_in_price,

    p.name AS program_name
FROM program.programs p
WHERE p.id = $1
`

type GetProgramRegistrationPricesForCustomerParams struct {
	ProgramID  uuid.UUID `json:"program_id"`
	CustomerID uuid.UUID `json:"customer_id"`
}

type GetProgramRegistrationPricesForCustomerRow struct {
	MemberProgramPrice    decimal.NullDecimal `json:"member_program_price"`
	MemberDropInPrice     decimal.NullDecimal `json:"member_drop_in_price"`
	NonMemberProgramPrice decimal.NullDecimal `json:"non_member_program_price"`
	NonMemberDropInPrice  decimal.NullDecimal `json:"non_member_drop_in_price"`
	ProgramName           string              `json:"program_name"`
}

func (q *Queries) GetProgramRegistrationPricesForCustomer(ctx context.Context, arg GetProgramRegistrationPricesForCustomerParams) (GetProgramRegistrationPricesForCustomerRow, error) {
	row := q.db.QueryRowContext(ctx, getProgramRegistrationPricesForCustomer, arg.ProgramID, arg.CustomerID)
	var i GetProgramRegistrationPricesForCustomerRow
	err := row.Scan(
		&i.MemberProgramPrice,
		&i.MemberDropInPrice,
		&i.NonMemberProgramPrice,
		&i.NonMemberDropInPrice,
		&i.ProgramName,
	)
	return i, err
}
