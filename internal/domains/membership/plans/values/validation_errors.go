package values

const (
	errEmptyName             = "name cannot be empty or whitespace"
	errNameTooLong           = "name cannot exceed 100 characters"
	errEmptyMembershipID     = "membership ID is required"
	errInvalidPrice          = "price must be greater than 0"
	errEmptyPaymentFrequency = "payment frequency is required"
	errInvalidAmtPeriods     = "amount of periods must be greater than 0"
)
